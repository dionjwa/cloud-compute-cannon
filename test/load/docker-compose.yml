version: '2'

services:

  loadtest:
    image: hakobera/locust
    ports:
      - "8090:8089"
    volumes:
      - ./:/test
    environment:
      - LOCUST_MODE=standalone
      - SCENARIO_FILE=/test/locustfile.py
      - TARGET_URL=http://ccc-internal.dev.lifesciences.autodesk.com


