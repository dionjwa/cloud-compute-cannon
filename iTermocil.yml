windows:
  - name: work
    root: ~/Documents/git/cloud-compute-cannon
    layout: main-vertical
    panes:
      - git status && git branch
      - subl cloudcannon.sublime-project
      - docker-compose up
      - cd ../metapage/docs && docker-compose up
