windows:
  - name: work
    root: ~/Documents/git/docker-cloud-compute
    layout: main-vertical
    panes:
      - git status && git branch
      - subl dcc.sublime-project
      - docker-compose up
