windows:
  - name: work
    root: ${GIT}/docker-cloud-compute
    layout: main-vertical
    panes:
      - git status && git branch
      - docker-compose up
      - make server && make webpack-watch
